---
type: 'post'
title: 'The title, as it reads on the page and in the tab'
description: 'One sentence. This is the SEO meta description.'
tags: ['nodejs']
publishedAt: '2026-01-01'
# repository: 'https://github.com/…'   # optional, renders a repo link
# draft: true                          # while it is being written
---

The body starts here. Headings, paragraphs, lists, links, inline code, fenced
blocks, quotes, images and rules are styled; anything else renders unstyled on
purpose, which is a request for a decision in `app/app.css` rather than an
oversight.

Copy this to `app/content/blog/<slug>/<slug>.en.md`, where `<slug>` is both the
folder name and the filename. That slug is the URL, and it never changes once
published.
